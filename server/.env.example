# Aim Dojo verify server — environment (set these in Railway → Variables)
SUPABASE_URL=https://hgvnytaqmuybzbotosyj.supabase.co
SUPABASE_SERVICE_KEY=eyJ...        # service_role key: Supabase → Project Settings → API. SECRET — never put this in the browser/repo.
ALLOW_ORIGIN=*   # CORS only gates browsers; validation+rate-limit are the real guards. Was the Pages origin until the Vercel move (2026-07-05) -- the app now lives on aim-dojo.vercel.app AND robjohncolson.github.io/aim-dojo
PORT=8080

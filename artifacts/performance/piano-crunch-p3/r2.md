# R2 trim benchmark — 2026-09-05

The optimized extracted runtime trim preserved all four original P0 fixture input/output hashes and the independent timestamp/family/index oracle. The two capped synthetic cases fell from 1.82–2.08 seconds to 4.96–6.02 milliseconds median in Node. This is an isolated CPU microbenchmark, not a browser frame, localStorage, finalizer, or natural-play-frequency measurement.

| Fixture | Before median / max ms | Fast median / max ms | Whole-wrapper stringify calls before → fast | Fast row stringify calls |
| --- | ---: | ---: | ---: | ---: |
| quiet-shaped-10min | 0.270 / 0.320 | 0.140 / 0.278 | 1 → 1 | 0 |
| busy-shaped-10min | 0.512 / 0.623 | 0.311 / 0.566 | 1 → 1 | 0 |
| fully-capped-2h | 1822.281 / 2061.133 | 6.019 / 7.194 | 857 → 2 | 856 |
| fully-capped-with-mail-and-ties | 2075.903 / 3057.996 | 4.955 / 7.577 | 870 → 2 | 869 |

Before and fast cases each use one warm-up and seven timed repetitions. Native JSON serialization is used during timing; input cloning, validation, hashing, VM construction, writes, and serialization instrumentation are outside timing. GC inside a timed call remains included. The small under-limit timing differences do not establish a separate speedup: both implementations do the same single wrapper serialization.

The two capped outputs remain 99,966 and 99,975 UTF-16 code units. Their saved P0 SHA256 hashes are `2c6ef1777a34a7dfa2bb32db0c75634a73f992980248f41abd596ff0315ea365` and `87e34a503e4244a5b787dd541c047c7fde2a23aa9b37edd8b807c073a8cd935e`. Mail is retained unchanged, and each retained event family is an unchanged suffix. The tool also checks the explicit equal-time BPM-first oracle. The original P0 tool/data in the primary workspace were untouched.

The quiet/busy outputs remain 9,690 / 20,896 code units and are byte-identical to their inputs. Their saved P0 hashes are `89c9a71ec248b0d259abe3b99cad0c79d8b3e6b1c453a8b5510d8a9f60d4d875` and `07288ef35ed9f8701a5d63eef1fa6ede6474b7e50107e2c1fa656bee1d997d0a`. All four exact hashes passed before, fast, and off-control runs.

Do not describe the result as fewer total stringify calls: capped total calls become 858 / 871 because small row serializations replace repeated whole-payload serializations. Total serialized code units fall from 94,118,866 → 237,888 and 95,357,116 → 237,149. The fast whole-wrapper contribution is 219,338 / 218,984 code units; row contribution is 18,550 / 18,165.

The current-source `--fast 0` control reproduces all saved bytes and the original 857 / 870 whole-wrapper serializations, with zero row serializations. Its one timed capped sample was 2942.251 / 2237.974 ms. This is a correctness/branch control, not another seven-sample performance comparison.

Source identity:

- Before runtime was copied before the R2 edit to [r2-before/runtime.js](r2-before/runtime.js). SHA256 `386ee200974b2bd8a1d522c8dc9f5fe8ee6a8a1096a98b454cc419d917362723`; it exactly matches HEAD `36f414a286a89bf20ddfd6217dd7d327c5199780` at measurement time.
- Fast and off-control runtime SHA256 `6ed10354367e2e60ab7dffc4221f4b287ea983cd3e7aae0a3acafb5ca2ed311b`; this is the uncommitted R2 runtime over that HEAD. Both reports identify it explicitly as different from the HEAD blob.
- Tool SHA256 `924f4085e04778f50a4ce009cd4b17b2c2f0b4b31e72d1334ebadb52d48466a8`. The extracted VM provides `CFG.ghostTrimFast` as numeric 0/1 and `GH_FAST_TRIM=(CFG.ghostTrimFast!==0)`; it never loads the page or reads user state. `node --check tools/piano-crunch-trim.mjs` passed.

Raw results, exact samples, environment, helper hashes, fixtures and removal order are in [before report](r2-before-benchmark/report.json), [fast report](r2-after-benchmark/report.json), and [off-control report](r2-off-control/report.json).

Commands run from the isolated checkout (choose fresh output directories when repeating):

```powershell
node tools/piano-crunch-trim.mjs --root . --source artifacts/performance/piano-crunch-p3/r2-before/runtime.js --fast 0 --runs 7 --warm 1 --out artifacts/performance/piano-crunch-p3/r2-before-benchmark
node tools/piano-crunch-trim.mjs --root . --fast 1 --runs 7 --warm 1 --out artifacts/performance/piano-crunch-p3/r2-after-benchmark
node tools/piano-crunch-trim.mjs --root . --fast 0 --runs 1 --warm 0 --out artifacts/performance/piano-crunch-p3/r2-off-control
```

No browser, GPU, or other measurement process was started by this diagnostic. The serialized benchmark jobs have finished.

Pre-edit runtime impact reported by the implementing agent: LOW, one direct caller, three upstream symbols, zero indexed execution flows. The benchmark helper's own impact was LOW with one direct caller and zero indexed flows; its serializer instrumentation was LOW with no indexed callers. Manual scope review confines tool changes to this standalone diagnostic. No runtime, test, mirror, or user-state file was edited by the benchmark agent.

Deviations: none for R2. Final full suite: 539/539 passed, including nine new trim tests. The new coverage authenticates the complete off-path function, both capped P0 hashes, exact boundaries, ties, unsorted heads, Unicode code units, property order, mail and retained array/row identity. Both comment-swallow scans printed 0; runtime/tool syntax and generated mirrors passed. The earlier-bloom tests and knobs are unchanged. Full TAP is in the primary workspace at artifacts/performance/piano-crunch-p3/r2-tests-final.tap. Runtime change: 20 inserted lines.

Final staged GitNexus detection: HIGH, 73 symbols / 10 flows across nine files. The standalone diagnostic contributes most symbols; stale mirror offsets flag eight neighboring runtime functions whose complete source bodies were verified unchanged against the preceding commit. Actual runtime edits are the trim function plus its two flag declarations.

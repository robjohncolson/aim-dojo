# The runtime stays one browser asset; generated parse-sized mirrors retain its graph.
aim-dojo-main.js
# Captures contain historical runtime copies; only index the live generated mirrors.
artifacts/
state/
